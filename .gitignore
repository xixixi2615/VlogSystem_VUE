# 依赖包（必写，忽略 node_modules）
node_modules/
# 构建产物（Vue 打包后的文件夹）
dist/
build/
# 敏感环境变量（本地配置，不上传）
.env.local
.env.development.local
.env.production.local
# 日志和缓存文件
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.cache/
# 编辑器配置（可选，根据自己的编辑器加）
.idea/       # WebStorm 等 JetBrains 编辑器
.vscode/     # VS Code（如果团队共用配置可不上传）
*.swp        # Vim 缓存
*.swo
# 系统自动生成的文件
.DS_Store    # Mac 系统
Thumbs.db    # Windows 系统